{
  "name": "lia-kachel",
  "version": "0.0.1",
  "description": "LiaScript Kachel plugin — drag-and-drop tile quizzes with touch support, cross-root drop emulation, and quiz freeze",
  "scripts": {
    "build": "parcel build src/index.ts --dist-dir dist --no-scope-hoist"
  },
  "devDependencies": {
    "parcel": "^2.16.4",
    "typescript": "^5.4.0"
  }
}
